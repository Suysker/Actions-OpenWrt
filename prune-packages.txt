# Package directories to remove before OpenWrt Kconfig scans package/.
# One package directory basename per line. Blank lines and comments are ignored.
#
# Use this only for packages with broken metadata or packages that must not
# appear in menu defaults at all. Normal package blocking belongs in
# forbidden-packages.txt.

luci-app-fchomo
luci-app-dockerman
luci-theme-alpha
luci-app-ssr-plus
shadowsocks-rust
