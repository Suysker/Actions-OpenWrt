# Lessons

## OpenWrt 配置与构建架构

- 文档连续性规则：简化 checker、报告或 workflow 时，只能删除重复实现，不能同步压缩掉产品决策、设备假设、模块接口、配置所有权、上游取舍、回滚和风险依据。对 `build-architecture.md` 做大幅重写前，必须审计从初始方案到当前实现的完整历史，将内容逐项分类为“仍有效、已演进、已被明确替代”；以机器可读声明保存精确清单，以架构文档保存原因和边界，不能用代码更短代替知识完整。
- 根因模式：feed 漂移后，虚拟 provider、内嵌翻译和历史包别名可能不再对应真实 Kconfig symbol。预防规则：所有 profile 必须经过 `make defconfig` 后的 seed drift、required 和 forbidden 三类校验；修正包契约，不放宽检查。
- 根因模式：失败后的 `!cancelled()` 发布/清理步骤会制造次生错误，甚至影响有效 Release。预防规则：构建、产物验证、发布和清理使用严格的成功依赖；只有新 Release 已创建并重新校验后才能清理旧版本。
- 架构规则：用户明确要求继续追踪 Lean `master`、保留 firewall3/iptables，并一次性交付 R4S 与 N5105 优化。后续不得在未获得新产品决策前改为官方稳定分支、firewall4/nftables 或分阶段上线。
- 可复现规则：追踪远程 master 或最新稳定 release 时，每次 workflow 启动只解析一次所有 ref/release，立即冻结完整 SHA、精确版本、不可变 URL 和 SHA256，随后双平台只消费同一 source-lock，并把解析结果随固件发布。“最新”是解析策略，不是跳过 hash 的理由。
- 优化规则：只纳入能由当前公开源码独立表达并能通过双 profile 配置、源码语义与成品验证的优化。私有 target、远程脚本、耦合内核补丁和只能依赖外部设备基准才能决定的候选不进入生产路径。
- 硬件边界规则：仓库方案、实施顺序和完成条件只能包含本地或 GitHub Actions 能执行的验证，不得把用户的 R4S、N5105/PVE、局域网、远程访问或性能数据假定为项目测试资源，也不保留所谓“可选真机测试”章节。设备外部行为不能自动证明时，选择一套明确、保守的 production 配置并如实限定构建证据范围。
- 设备调优规则：先审计 target/device 已有的 DTS、hotplug、UCI defaults 和驱动选择。R4S 已把两个网口 IRQ 分配给 CPU4/CPU5；通用 irqbalance 会产生第二个所有者并可能撤销设备优化，因此设备原生策略优先，重复调优必须删除。
- 配置包规则：`default-settings` 一类名称不能被视为无害基础包。选择前必须审读全部首次启动副作用；软件源、固定密码、签名、防火墙、OTA 和 steering 等跨域行为应拆成项目自己的窄 overlay，并把原整包加入 forbidden/manifest 契约。
- 上游移植规则：先比较当前基座，再判断“版本替换”是否真是升级。sbwml 的 boot chain 比当前 Lean 更旧，r8168 的小版本 bump也缺少当前问题证据；已有更新、依赖更完整的 native 实现应保留。
- Kconfig 规则：第三方 config 中看似合理的 symbol 也可能无效。当前 R4S zram 必须同时选择 `CONFIG_KERNEL_ZRAM_BACKEND_LZ4`、`CONFIG_KERNEL_ZRAM_DEF_COMP_LZ4` 和实际依赖 `kmod-lib-lz4`；Lean 6.18 曾因 package config 只在 `LINUX_6_12` guard 内声明 backend 而让 defconfig 丢弃前者。遇到这种跨系列上游缺口，不得删除功能合同或替换整份 modules 文件：由同一 source-lock 的 selected series 驱动最小、幂等 guard 扩展，上游已原生支持时 no-op，条件结构漂移时失败。N5105 的 `CONFIG_VIRTIO_SUPPORT` 则是不可见的 target 内部 symbol，不能写进 seed，应该检查 x86 kernel config 中真实 built-in 的 `CONFIG_VIRTIO_NET`/`CONFIG_SCSI_VIRTIO`。所有优化 symbol 必须以当前 source-lock 执行 `make defconfig` 后证明仍然存在。
- 内核通道规则：Linux upstream 的 longterm 身份不等于 Lean target 的 stable 身份。stable/testing 意图只由 common 渲染 Kconfig 声明，一个共享 resolver 根据锁定 Lean 的 `KERNEL_PATCHVER`/`KERNEL_TESTING_PATCHVER` 为每个 target 解析 channel、series、精确 version/hash，再投影给 source-lock、patch applicator、profile contract 和 provenance；消费者不得各自用正则重解。仓库只固定共同 channel，不覆盖 Lean 的 point release/hash，也不强迫不同 target 永远使用同一 series；任一 profile 缺少对应 BBRv3 port 或兼容语义时整轮失败，不能静默降级。
- LuCI 翻译规则：当前 LuCI 的 `luci-i18n-*-zh-cn` 是随应用生成的隐藏 package symbol，不能作为逐项 seed 输入；common 应选择公开的 `CONFIG_LUCI_LANG_zh_Hans=y`，再由 required manifest 和镜像清单验证实际翻译产物。遇到 seed drift 必须检查 symbol 的 prompt/default/dependency，不能把隐藏输出当作用户配置入口。
- 精简配置规则：forbidden 清单如果只做事后断言，Lean target 的默认包仍会先进入 `.config`。`exact:` package 必须由共享 renderer 自动生成 Kconfig 负选择，再依次通过 seed drift、forbidden config 和最终 manifest 门禁；regex 只能做集合断言，当前已知的 target 默认项还要有 exact 规则作为配置输入。不要在每个设备 seed 手抄同一批排除项。
- Profile 事实源规则：checker、workflow 或测试里再次枚举 profile 名、包名、CPU flags、网络字段、UCI 命令或 Kconfig 值，会让需求变更必须同步修改声明与代码；即使没有 `if r4s`，required/forbidden 各走一套解释器或中央语义文件再次映射设备，也仍是双重维护。必选 package/布尔 config、exact 禁用 package、其他 Kconfig、env 与仓库自有 rootfs 行为必须分别只有一个声明所有者；rootfs 脚本或配置文件本身就是唯一事实源，禁止在 `semantics.json`、测试或 checker 中逐行镜像其内容，只由通用 renderer、冲突检测和语法检查验证交付边界。`semantics.json` 仅描述仓库之外、会随本轮锁定 Lean/feeds 漂移的上游源码能力；设备语义与设备目录同置，common 语义只位于 common。统一 `ProfileRepository` 从目录发现 profile、合并 common/device、派生正负选择并一次评估最终 package contract，shell 只适配 CLI；新增 profile 或组件不得要求修改 checker、matrix、update checker 或测试分支。
- 跨平台工作树规则：Windows `core.autocrlf` 会把可执行 Shell/Python、OpenWrt patch、Kconfig seed 与无扩展名的 UCI/hotplug 脚本检出为 CRLF，造成 WSL 本地测试或 Linux CI 与仓库 blob 行为不一致。只用窄 `.gitattributes` 为这些运行输入固定 LF；不要为了修一个脚本批量改写全部文本文件，也不要在共享解析模块里复制已有的 `profile_model.parse_config`。直接按文件加载模块的测试必须加入同一个 `scripts` import root，以验证真实依赖图。
- Kconfig 子选项规则：父 LuCI app 为 `n` 不代表其所有 `INCLUDE_*` symbol 都会自动变成 `n`；缺少父依赖的子选项或独立 choice 仍可能通过 `select` 拉入被禁 package。不能随着每次泄漏手写一个 `INCLUDE_*`：首次 `make defconfig` 后只针对 exact-forbidden 父包统一归一化选中子 symbol，并在本轮生成的 package Kconfig 的对应 config/choice 上补父依赖 guard，再次 defconfig 并复验；已确认被夹带的 package 仍加入最终 blacklist。允许应用的子选项不得被触碰。
- CPU 规则：微架构名不等于具体 SKU 的完整 ISA 合约。N5105 production 固定使用 `x86-64-v2` 与 `-mtune=tremont`，不引入严格 `-march=tremont`、自动 fallback 或 Tremont 不支持的 `x86-64-v3`/AVX。R4S 不照搬只偏向 A72 的 kernel `-mcpu`，也不新增未经现有构建合同证明安全的 kernel-specific flags。
- 网络队列规则：硬件/虚拟 multiqueue、RPS/XPS、手工 affinity 和 irqbalance 都会影响包落在哪个 CPU。每个平台只保留一套明确所有权：R4S 使用 native IRQ affinity + Lean 全核 packet steering，不叠加 irqbalance 或 A72-only 覆盖；N5105 使用 4 queues + irqbalance，并关闭 RPS。不得在仓库积累候选矩阵、调优开关或 fallback。
- DNS 组合规则：安装 dnsmasq、AdGuardHome、MosDNS、SmartDNS 和 PassWall 时，运行时必须能解释唯一的 LAN `:53` 有效入口、完整转发图和各层缓存行为；不强制所有服务只监听 loopback，也不擅自把用户的多级缓存改成单一所有者，但必须证明没有端口争用、WAN 暴露和可触发的查询环路。
- DNS 诊断规则：配置文件里的 listener 不能单独证明真实流量路径。AdGuardHome 等 LuCI 集成可能通过 iptables PREROUTING redirect 保留 dnsmasq `:53`、同时把 LAN 查询送到另一端口；必须联合检查 UCI、服务 YAML、init/firewall 脚本、实际 socket 和 NAT 规则。
- 构建边界规则：固件负责包、provider、安全默认和兼容性 contract；用户常用 DNS 的端口、upstream、缓存、过滤规则、PassWall 节点/订阅及凭据属于可变运行时状态，只用于设备配置与验收，不进入跨设备镜像或仓库。
- 源码补丁准入规则：历史 `diy-part2.sh` 的 `sed`、旧注释或用户常用运行值都不能单独证明源码补丁有必要。必须先对照本次 source-lock 的上游实现和现有 UCI/规则接口；已经失效、已被上游覆盖或能由运行时配置表达的改动不进入 patchset。需要持续追新的内核能力应在仓库保存受信任 provider/ref/path 策略，每轮解析 selected series 的兼容 port、物化并冻结 commit/hash，再对精确 kernel clean-apply；缺少匹配适配时失败，不能套用其他内核版本、越过声明擅自换 channel 或静默回退。真实 provider 可能使用 Git format-patch 或没有 `diff --git` 的 OpenWrt/quilt unified diff，resolver 与 checker 必须复用同一安全 path parser，测试不能用单一伪格式替代真实输入。
- BBRv3 与运行时所有权规则：BBRv3 是修改通用 TCP core 的版本化内核能力，不是普通插件，也不因源码路径在 `net/ipv4` 就只支持 IPv4；IPv6 TCP socket 同样使用 `tcp_init_sock()`/`tcp_congestion_ops`，但 UDP/QUIC 和普通 NAT 转发连接不由路由器本机 CCA 接管。common 保留 Lean 的 `kmod-tcp-bbr`、`tcp_bbr.ko` 和运行名 `bbr` 契约，以 source-lock、patch SHA256、源码 `BBR_VERSION=3` 和 build 直接读取每个 cross-built ELF `.modinfo` 的 version/vermagic 证明代际；这样可直接复用 TurboACC 的上游依赖。Lean 的 `CONFIG_MODULE_STRIPPED` 会把普通 `MODULE_VERSION` 变成禁用声明，所以 provider patch 中出现该宏不能证明成品有版本字段；应保留全局 strip，只由一份声明合同和共享解释器在需要时后置 direct `MODULE_INFO(version, ...)` companion，上游已保留时自动跳过，歧义时失败。外部验证器错误不得用 `2>/dev/null || true` 折叠为 `missing`，必须保留候选路径、原始错误和可复核样本。一次性 UCI 脚本只在确认 version `3` 后设置 factory default 并写持久标记，sysupgrade 不覆盖用户后来选择的 CCA；后续 TCP CCA 和 software flow offload 由 TurboACC/UCI 唯一管理。
- qdisc provider 规则：写入 `net.core.default_qdisc=fq` 不代表内核已经提供 `fq`；`fq_codel` 也不是 `fq`。必须从当前 source-lock 的 kernel package 定义确认 provider，common 显式选择它，并在构建树与成品中检查 `sch_fq.ko` 及 vermagic，不能只验证 sysctl 文本。
- 上游产物规则：HAProxy LTS、AdGuardHome stable、GeoIP 和 Geosite 可以自动追最新，但 resolver 必须使用发布方的 SHA256/checksum/asset digest，并把精确结果写入 source-lock；package applicator 不访问网络，不允许 `PKG_HASH:=skip` 或 `latest/download`。GeoIP/Geosite 的角色、可信仓库、asset 与 package 字段属于静态供应链/接口合同，只能在一份声明式文件中定义，resolver、validator、applicator 必须共用，不能在执行函数里重复 tuple：当前由 `Loyalsoldier/geoip` 提供 `geoip.dat`，`Loyalsoldier/v2ray-rules-dat` 提供 `geosite.dat`，`v2ray-geodata` 只负责同时打包二者。release/tag/hash 属于每轮动态状态，不进入静态合同。设备内二进制自更新不得绕开 OPKG、非特权 jail、provenance 和 Release 回滚。
- Feed provider 规则：同一 package 同时存在于默认与自定义 feed 时，不能依赖 feed 遍历顺序。custom 可以用同名 feed 显式替换 default，但 incoming lock 必须逐项匹配 custom name/type/URL/ref/origin；只有真实 package provider 冲突才允许删除合同列出的 source directory，随后必须从 source-lock 枚举并重建全部 feed index。selector、artifact applicator 和最终 profile checker 共用一份 provider contract。历史 owner URL 已迁移时使用 GitHub 返回的 canonical 仓库（当前 PassWall 为 `Openwrt-Passwall/*`），不依赖重定向或已删除的旧入口。
- Feed 安装闭包规则：`feeds install -a` 会把未选应用和 telephony 等全部 Makefile 注入 Kconfig；未选包自身的递归依赖或缺失 provider 也会污染 `make defconfig`，这属于实际构建影响，但修这些未使用包会错误扩大产品维护面。provider 选择/重建索引后，只把当前 profile 的 required package 交给 OpenWrt feeds installer，让其递归安装真实 source/build/runtime dependency；不得维护或安装与两套 profile 无关的 feed source。
- 用户网络默认规则：用户已明确 common 地址池从 `.32` 开始且 `limit=232`；LAN 与 WAN 的 RA/DHCPv6/NDP 都使用 relay，WAN 是唯一 master，LAN 同时保留 `delegate=0`/`ip6assign=64`。这些字段必须由同一个 rootfs 脚本一次写全，不能拆成另一份 semantics/测试镜像，也不能只改一个 UCI 字段造成半配置状态。其余 WAN 地址、桥接和端口偏好不从历史配置扩大推断。
- 构建优化规则：运行性能优化、固件体积优化和链接速度优化必须分开评价。LTO/GC sections 是实验项，Mold 主要缩短链接时间；不能为了“全开”让持续 master 的生产构建积累单包 opt-out 和隐性 fallback。
- 继承优化合同规则：文档写“保留 Lean native 优化”而 checker 只验证 overlay 存在，会让 master 漂移后静默丢失实际能力。common、R4S、N5105 的稳定性能意图必须集中声明并检查：仓库自有 rootfs 行为直接由所属文件表达，经通用渲染、冲突与语法边界验证，不另存一份内容断言；本轮锁定 Lean tree/feeds 的外部源码能力在 defconfig/prepare 后由 `semantics.json` 做内容检查。kernel series 动态展开，能力既可由 selected Lean backport 提供，也可在 prepared upstream kernel source 中等价存在，二者都无完整语义时失败，不永久钉死版本、commit/hash 或 patch 文件名。共享声明必须进入每个平台的 profile digest，设备私有目录仍只影响自己的 digest。仓库只陈述配置、源码和成品能够证明的能力，不设置外部设备性能晋升流程。
- GCC 与 package 兼容规则：用户明确要求把 `CONFIG_GCC_USE_VERSION_15=y` 作为稳定工具链契约写死；不得改成 Lean 默认 GCC13、自动最高 major 或失败回退。当一次配置闭包中的 Lean packages 连续暴露同类陈旧基建时，应先按最终 `.config`/构建日志界定真实 package 集，再选择维护更活跃且完整兼容的 canonical feed，而不是逐包维护私有补丁。当前产品的通用 packages 整体追踪并锁定 `openwrt/packages@master`；官方 packages 不提供而当前依赖需要的 PCRE2，以及已经由官方 core 解决 GCC15/C23 问题的 GMP、MTD 和 `f2fs-tools`，从同一锁定的 OpenWrt core 窄同步。`mkf2fs` 把旧 `f2fs-tools` 拉入闭包后，GCC15/C23 会拒绝其 `typedef u8 bool`；应复用官方 `stdbool.h` 修复所在的完整 canonical 子树，不能为它单独降到 GNU17、删除启动依赖或固定 package 版本/hash。overlay manifest 是映射唯一事实源，resolver 和 fixture 必须从它动态派生，不能在测试中再抄 package 清单；target 安全策略按 `package/`、`feeds/` 根和最小深度表达，不枚举具体分类。只有 canonical 来源也未解决或产品明确选择旧 provider 时才评审语义兼容：当前仅 `libsepol`、旧 `wol` CLI 和 current `small/tcping` 需要该路径。包级汇总错误只用于定位候选包；最终以定向 `-j1 V=sc` 首条诊断、完整 download/hash 和双平台构建闭环。不得为过编译删除 required package、处理未进入构建闭包的包、全局降级 GCC、关闭 `-Werror` 或放宽错误。
- 版本锁语义规则：仓库永久写死的普通上游版本/hash、当轮 source-lock 和上游 feed recipe 的 metadata 是三类不同事物。普通最新组件不得在仓库永久钉死；prepare 必须每轮解析最新并生成精确 URL/hash，build 当轮必须严格校验。用户明确的功能代际/ABI（GCC15、BBRv3、N5105 `x86-64-v2` 基线）可以作为稳定契约。fixture 中的假 commit/hash 和历史审计 commit 只用于确定性测试/依据记录，不能进入生产解析路径。
- CI 缓存规则：ccache、下载缓存和 toolchain 缓存必须分层。key 至少绑定架构、编译器、profile、patch 和 source-lock；不恢复 fork 缓存，不使用无完整 identity 的第三方预编译工具链。
- Actions runtime 规则：用户明确优先直接跟踪最新版本，因此 workflow 只允许官方 `actions/*@main`，不保留静态 action lock、tag 或 SHA。Action 在 workflow 开始时已经由 GitHub 解析，运行中观察远端 HEAD 既不能证明已执行字节，也不应进入 source-lock/update fingerprint；任何上游 runtime 或行为漂移必须由真实构建与交付门禁暴露。扫描器必须同时识别 YAML 的 `uses:` 映射行与 `- uses:` 行内列表形式。
- Release 授权规则：GitHub Release 指向的 commit 若相对默认分支增删或修改 `.github/workflows/*`，内置 `GITHUB_TOKEN` 无法获得所需的 workflows 写权限，即使 job 日志显示 `Contents: write` 也会在创建或更新 Release 时返回 403。不得把 tag 错挂到默认分支或放弃真实 build commit；profile 选择与发布授权必须分离，默认分支使用内置 token，非默认分支只在 Release jobs 使用明确授予仓库/workflow 写权限的已配置凭据，任意分支的全 profile 构建都进入同一 Release 事务。draft Release 只对具有 push 权限的调用者可见，因此创建、按 tag 回下载、公开和清理四个 Release job 都必须声明 `contents: write`；回下载 job 使用 `contents: read` 时，GitHub 会把真实存在的 draft 表现为 `release not found`，不能用 sleep、重试或绕回 Actions artifact 掩盖授权错误。
- 交付命名规则：Actions artifact 中同时保留 `sha256sums` 和 `SHA256SUMS` 会在 Windows 大小写不敏感文件系统上解压冲突；上游校验表必须在交付边界规范化为 `openwrt-sha256sums`。Release 是用户产品界面，不得暴露 `<profile>--<internal-file>` 式内部映射；直接刷写镜像使用稳定 kebab-case 专业名，其余 provenance 按 profile 收敛到一个完整包，回下载 verifier 证明直接镜像与包内原件一致。
- Release 展示规则：BBRv3 的 provider、模块 metadata 与版本断言属于内部实现和验收证据；公开 Release 说明只展示内核版本或稳定内核策略，不宣传 BBRv3 模块代际。内部 source-lock、provenance、架构文档和验证器继续保留真实技术合同。
- 校验收益规则：同一事实被 workflow、shell、Python 和多个 report 分别解释，会造成 schema 升级与 profile 变化时的多头维护，校验数量本身也不等于可信度；把仓库实现中的精确文本复制到断言里，会制造第二个事实所有者，并不构成独立证据。生产链只保留四个高价值边界：浮动输入冻结、最终 config、固件交付、Release 回下载；每个边界只有一个实现，后续阶段复用前一阶段的正式接口。结构化事实集中到一个 provenance，原始日志只在失败时作为 diagnostics；无人消费的成功报告、弱尺寸启发式和紧邻完整校验的定向重复检查不得进入正式路径。
- 自定义 package recipe 规则：feed 中的 `define Build/Compile` 一旦自行调用 `$(MAKE)`，就可能绕过 OpenWrt 默认 `MAKE_FLAGS` 所传的 `$(TARGET_CONFIGURE_OPTS)`，从而让 `STRIP` 等工具错误回退到宿主机。不能因 PassWall 的硬依赖而删除 current `small/tcping` 或降回旧 provider；应以 common 声明式兼容 manifest 在唯一 make 语义锚点后幂等补回完整目标工具链环境，断言原 CC/CFLAGS/LDFLAGS 仍在同一调用中，并在上游修复或 recipe 漂移时分别记录/失败。该规则不得依赖 package version、hash 或 release URL。
- 并行构建诊断规则：`make -j` 的包级汇总并不足以定位根因，而失败后完整串行重编 `world` 会浪费数小时且掩盖可操作反馈。CI 应从并行日志提取第一个受严格路径校验的 `package/...` 目标，仅以 `make -j1 V=sc <target>/compile` 收集详细诊断并保持原 job 失败；日志不含安全目标时才允许 whole-world 串行回退。OpenWrt 会在稳定的 `ERROR: package/... failed to build` 语义前缀后追加 `(build variant: default).` 等说明，解析器应匹配前缀并继续对白名单路径做独立校验，不能依赖句点紧邻 `build` 的旧格式。该解析/重放逻辑必须有覆盖当前真实格式的独立 fixture，不能直接把并行失败当成某个 package 的永久版本补丁依据。
- 固件容器格式规则：OpenWrt 的 `.img.gz` 可以是纯 gzip，也可能在完整 gzip member 后追加正式的 fwtool INFO、SIGNATURE 或 INFO→SIGNATURE；GNU `gzip -t` 会把这些合法 chunk 报成 `trailing garbage` 并返回 2。不能用 `|| true` 放宽，也不能只验 gzip payload：共享解释器必须定位精确 gzip EOF，再对存在的 trailer 按 OpenWrt ABI 验证 magic、zero pad、类型/唯一顺序、size 上限、累计 raw CRC32、metadata header version 与 JSON；flags 等上游声明为透明的字段只由 CRC 保护，不在下游重复收紧。任意未解释字节仍失败。布局由实际字节决定，build 与 Release 回下载复用同一入口，不为 R4S/x86 分别判断。
- 构建产物生产链规则：最终 `.config` 中某个 feature 为 `y` 只证明 Kconfig 选择成立，不能证明下游 fork 同步了对应 Make 规则、生成器及其依赖。对 manifest、SBOM、buildinfo 等强制产物，必须逐层验证“开关→生产规则→工具能力→最终文件”；不能删除门禁或伪造空文件。若 Lean 保留开关却漏掉上游实现，应从同一轮锁定的 canonical core 以显式 `tree`/`file` 边界同步最小依赖到隔离命名空间，再通过共享语义兼容接口补齐稳定调用；不得覆盖整份 target 核心文件。执行器必须识别完整的上游等价实现并 no-op、拒绝半同步状态，fixture 从同一声明动态派生并同时验证 file 内容与 Git executable mode，避免映射、调用文本和测试三头维护，也避免脚本字节正确却不可执行。
