# Forbidden package selections for the PVE OpenWrt VM image.
# Blank lines and comments are ignored.
#
# Formats:
#   prune:<package-or-source-directory-name> removes a broken/unwanted package entry before Kconfig and forbids selection
#   exact:<package-or-source-directory-name>
#   regex:<extended-regular-expression>
#   <plain text> is treated like exact:<plain text>

# Proxy/plugin packages that are not part of config.seed.
prune:luci-app-fchomo
prune:luci-app-dockerman
prune:luci-theme-alpha
prune:luci-app-bypass
prune:luci-app-ddns
prune:luci-app-ssr-plus
prune:luci-app-rclone
prune:luci-app-vlmcsd
prune:luci-app-vsftpd
exact:ddns-scripts
exact:ddns-scripts-services
regex:^ddns-scripts_
exact:mihomo
exact:sing-box
exact:trojan-plus
exact:simple-obfs
exact:v2ray-plugin
exact:vlmcsd
exact:vsftpd
exact:shadowsocks-rust
regex:^shadowsocks-
exact:luci-proto-wireguard
exact:kmod-wireguard
exact:wireguard-tools
exact:luci-proto-bonding
exact:proto-bonding
exact:kmod-bonding

# Physical NIC drivers not needed by this PVE VM.
# Keep CONFIG_VIRTIO_SUPPORT and kmod-igc in config.seed.
regex:^kmod-(8139cp|8139too|alx|amazon-ena|amd-xgbe|atlantic|bnx2|bnx2x|e1000|e1000e|forcedeth|i40e|i40evf|iavf|igb|igbvf|ixgbe|mlx4-core|mlx5-core|mlxfw|pcnet32|r8125-rss|r8126-rss|r8168|tg3|tulip|via-velocity|vmxnet3)$
regex:^(amd64|intel)-microcode$

# Firmware/data packages for unrelated physical hardware.
regex:^[A-Za-z0-9_.+-]+-firmware$
exact:usbids
exact:usbutils
# This helper is not the VirtIO disk/net driver.
exact:virtio-console-helper

# USB, HID, removable storage and extra filesystem support.
# Filesystem modules are not hard-forbidden; x86 images may pull them in.
regex:^kmod-usb
regex:^kmod-hid
regex:^kmod-drm-
regex:^kmod-sound-
exact:kmod-mmc
exact:kmod-sdhci
exact:ntfs3-mount

# Samba, automount and disk maintenance tools.
regex:^(autosamba|autosamba_.*|.*samba.*|ksmbd.*)$
exact:automount
exact:cfdisk
exact:e2fsprogs
exact:mkf2fs
exact:partx-utils

# Sensors and nonessential maintenance tools.
